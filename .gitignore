# This .gitignore file is used by Git to determine which files and directories to ignore when committing changes.

# Generated by Cargo
# will have compiled files and executables
# The `debug/` and `target/` directories are generated by Cargo (Rust's package manager) during the build process.
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# The `Cargo.lock` file is used by Cargo to keep track of dependencies. It is typically ignored for libraries, but included for executables.
Cargo.lock

# These are backup files generated by rustfmt
# The `**/*.rs.bk` pattern matches backup files generated by rustfmt, Rust's code formatter.
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
# The `*.pdb` pattern matches Program Database (PDB) files, which are generated by the MSVC build of rustc on Windows and contain debugging information.
*.pdb

# Added by cargo
# The `/target` directory is added by Cargo and contains the output of the build process.
/target

# The `.env` file is used to store environment variables. It is typically not included in version control to avoid exposing sensitive information.
.env