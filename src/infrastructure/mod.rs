pub mod ipfs;
/// The `infrastructure` module contains the infrastructure services of the application.
/// It is organized into submodules, each of which provides a specific infrastructure service.
///
/// Currently, the `infrastructure` module contains the following submodules:
/// - `s3`: This module provides the service for interacting with Amazon S3.
/// - `ipfs`: This module provides the service for interacting with the InterPlanetary File System (IPFS).
pub mod s3;
