/// The `domain` module contains the business logic of the application.
/// It is organized into submodules, each of which contains a specific part of the domain logic.
///
/// Currently, the `domain` module contains the following submodules:
/// - `services`: This module contains the service layer of the application. The service layer is responsible for implementing the business operations.
pub mod services;
