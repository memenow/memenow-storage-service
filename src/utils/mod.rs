/// The `utils` module contains utility functions and modules that are used throughout the application.
/// It is organized into submodules, each of which provides a specific set of utility functions.
///
/// Currently, the `utils` module contains the following submodule:
/// - `file`: This module provides utility functions for file handling.
pub mod file;
